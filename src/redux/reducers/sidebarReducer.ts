
export const sidebarReducer = (state: any, action: ActionType) => {
    switch (action.type) {
        case 'XXX':
            return state
        default:
            return state
    }
}

type ActionType = any
